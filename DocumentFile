To-Do List API (up to date)

Register: JSON {
                “email”: String
                “password”: String
                “username”: String
		       }
Login: JSON {
		        “email”: String
			    “password”: String
	         }

Register/Login Return {result: String} ### "Pass" -> successful, anything else will be specific content

